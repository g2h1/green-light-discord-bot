{
  "name": "discord-manager",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend", "bot"],
  "scripts": {
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:bot": "npm run dev -w bot",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w bot && tsc -p frontend --noEmit"
  }
}
